# Also issue #1229

test "invalid function parameter type":
	parse:
		set {_list::*} to "hello" and "bye"
		set {_test} to max({_list::*})
	assert last parse logs is set with "Variable '{_list::*}' is of type text, and is not a number"
